package Text::ANSITable::ColorTheme::Default::no_color;

# AUTHORITY
# DATE
# DIST
# VERSION

use parent 'ColorThemeBase::Static::FromStructColors';

our %THEME = (
    v => 2,
    summary => 'Special theme that means no color',
    colors => {
    },
    no_color => 1,
);

1;
# ABSTRACT:
